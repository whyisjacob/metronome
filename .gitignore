# Xcode / build artifacts
build/
DerivedData/
*.moved-aside
*.xcuserstate
xcuserdata/

# XcodeGen generates the project — do not commit it.
# Run `xcodegen generate` on a Mac to (re)create Metronome.xcodeproj.
*.xcodeproj/
*.xcworkspace/

# Swift Package Manager
.build/
.swiftpm/

# CI screenshot capture (produced by .github/workflows/screenshots.yml on the runner)
*.xcresult
/screenshots/

# macOS
.DS_Store
