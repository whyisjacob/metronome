source "https://rubygems.org"

# fastlane drives the signed Release build + TestFlight upload in CI
# (see fastlane/Fastfile, lane :beta). Unpinned so CI picks up the latest
# App Store Connect API-key + cloud-signing fixes.
gem "fastlane"
